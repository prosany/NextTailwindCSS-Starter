// import { useAppSelector } from '@/redux/store';

// const useRTK = (func: any, options: any = {}) => {
//   const { tokens } = useAppSelector((state) => state.auth);
//   const queryData = func(options, {
//     skip: !tokens?.access?.token,
//   });
//   return queryData;
// };

// export default useRTK;
